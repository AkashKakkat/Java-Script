// spread operator {...} in JS is used to spread out the elements of the array or the properties of an object
// it allows copy / combine / expand

const arr1 = [1,2,3];
const arr2 = [...arr1];
console.log(arr2);
